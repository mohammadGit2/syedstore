import Link from 'next/link';import { categories } from '@/data/products';export const metadata={title:'Categories'};export default function Categories(){return <section className="container py-12"><h1 className="text-4xl font-black text-ocean">Categories</h1><div className="mt-8 grid gap-6 md:grid-cols-4">{categories.map(c=><Link className="card overflow-hidden" href={`/categories/${c.slug}`} key={c.slug}><img src={c.image} alt="" className="h-52 w-full object-cover"/><div className="p-5"><h2 className="font-black">{c.name}</h2><p className="text-slate-600">{c.description}</p></div></Link>)}</div></section>}
