import Link from 'next/link';import { getCatalog } from '@/lib/catalog';export const dynamic = 'force-dynamic';export const metadata={title:'Categories'};export default async function Categories(){const {categories}=await getCatalog();return <section className="container py-12"><h1 className="text-4xl font-black text-ocean">Categories</h1><div className="mt-8 grid gap-6 md:grid-cols-4">{categories.map(c=><Link className="card overflow-hidden" href={`/categories/${c.slug}`} key={c.slug}><img src={c.image} alt="" className="h-52 w-full object-cover"/><div className="p-5"><h2 className="font-black">{c.name}</h2><p className="text-slate-600">{c.description}</p></div></Link>)}</div></section>}
