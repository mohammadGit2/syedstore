import { notFound } from 'next/navigation';import { ProductCard } from '@/components/ProductCard';import { categories, products } from '@/data/products';export function generateStaticParams(){return categories.map(c=>({slug:c.slug}))}export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=categories.find(x=>x.slug===slug);if(!c)notFound();const list=products.filter(p=>p.category===slug);return <section className="container py-12"><h1 className="text-4xl font-black text-ocean">{c.name}</h1><p className="mt-3 text-slate-600">{c.description}</p><div className="mt-8 grid gap-6 md:grid-cols-3">{list.map(p=><ProductCard product={p} key={p.id}/>)}</div></section>}
