import { notFound } from 'next/navigation';import { ProductCard } from '@/components/ProductCard';import { getCatalog } from '@/lib/catalog';export const dynamic = 'force-dynamic';export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const catalog=await getCatalog();const c=catalog.categories.find(x=>x.slug===slug);if(!c)notFound();const list=catalog.products.filter(p=>p.category===slug);return <section className="container py-12"><h1 className="text-4xl font-black text-ocean">{c.name}</h1><p className="mt-3 text-slate-600">{c.description}</p><div className="mt-8 grid gap-6 md:grid-cols-3">{list.map(p=><ProductCard product={p} key={p.id}/>)}</div></section>}
