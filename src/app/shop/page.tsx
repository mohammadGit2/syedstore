import { ProductCard } from '@/components/ProductCard';import { categories, products } from '@/data/products';export const metadata={title:'Shop'};export default function Shop(){return <section className="container py-12"><h1 className="text-4xl font-black text-ocean">Shop Products</h1><div className="mt-8 grid gap-8 lg:grid-cols-[260px_1fr]"><aside className="card h-fit p-5"><h2 className="font-bold">Filters</h2><input placeholder="Search products" className="mt-4 w-full rounded-xl border p-3"/><label className="mt-4 block text-sm font-bold">Sort</label><select className="mt-2 w-full rounded-xl border p-3"><option>Featured</option><option>Price: Low to High</option><option>Newest</option></select><h3 className="mt-5 font-bold">Categories</h3>{categories.map(c=><a className="mt-2 block text-slate-600" href={`/categories/${c.slug}`} key={c.slug}>{c.name}</a>)}</aside><div><div className="grid gap-6 md:grid-cols-3">{products.map(p=><ProductCard product={p} key={p.id}/>)}</div><nav className="mt-10 flex gap-2" aria-label="Pagination"><span className="btn btn-primary">1</span><span className="btn btn-secondary">2</span></nav></div></div></section>}
