import Link from 'next/link';export function Breadcrumbs({items}:{items:{label:string,href?:string}[]}){return <nav aria-label="Breadcrumb" className="container py-5 text-sm"><ol className="flex flex-wrap gap-2 text-slate-500"><li><Link href="/">Home</Link></li>{items.map(i=><li key={i.label}>/ {i.href?<Link href={i.href}>{i.label}</Link>:<span>{i.label}</span>}</li>)}</ol></nav>}
