import Link from 'next/link';import { Product } from '@/types/product';import { formatPrice } from '@/lib/pricing';
export function ProductCard({product}:{product:Product}){return <article className="card overflow-hidden"><Link href={`/products/${product.slug}`}><img src={product.images[0]} alt={product.name} className="h-56 w-full object-cover"/></Link><div className="p-5"><p className="text-sm font-semibold text-sea">{product.stockStatus}</p><h3 className="mt-2 text-xl font-bold"><Link href={`/products/${product.slug}`}>{product.name}</Link></h3><p className="mt-2 text-sm text-slate-600">{product.shortDescription}</p><div className="mt-4 flex items-center gap-3"><strong className="text-lg text-ocean">{formatPrice(product.salePrice??product.price)}</strong>{product.salePrice&&<span className="text-sm text-slate-400 line-through">{formatPrice(product.price)}</span>}</div><Link className="btn btn-primary mt-5 w-full" href={`/products/${product.slug}`}>View Product</Link></div></article>}
